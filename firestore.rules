rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This rule allows anyone with your database reference to read or write to your entire database.
    // This should only be used for testing and should be changed before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}